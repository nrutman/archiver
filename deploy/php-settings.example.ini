; Recommended domain-level PHP values for Archiver.
; Prefer cPanel MultiPHP INI Editor on cPanel-managed Apache PHP-FPM and LiteSpeed LSAPI hosts.

upload_max_filesize = 100M
post_max_size = 220M
max_file_uploads = 30
max_input_time = 300
max_execution_time = 300
memory_limit = 512M
